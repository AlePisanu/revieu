# Changelog

All notable changes to this project are documented here.
Format based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/).

---

## [Unreleased]

### Planned
- Project setup (manifest, build pipeline, folder structure)
- GitHub adapter — diff, title and description extraction from DOM
- Sidebar UI with tone selector, provider selector, mode toggle
- Anthropic provider with SSE streaming
- Full context mode (raw GitHub, DOM expand, GitHub API)
- Gemini Flash provider (free tier)
- Dependency map — static import analysis between changed files

---

## [0.1.0] — in progress

### Added
- Initial folder structure
- `manifest.json` Manifest v3
- esbuild build pipeline (watch + prod)
- Background service worker for storage and messaging
- `.gitignore`, `package.json`
