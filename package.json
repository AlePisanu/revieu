{
  "name": "revieu",
  "version": "0.1.0",
  "description": "AI-powered code review sidebar for GitHub",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node build.js --watch",
    "dev:firefox": "node build.js --watch --firefox",
    "build": "node build.js && node build.js --firefox",
    "build:chrome": "node build.js",
    "build:firefox": "node build.js --firefox",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.38",
    "@types/dompurify": "^3.0.5",
    "@types/marked": "^5.0.2",
    "esbuild": "^0.25.0",
    "typescript": "^5.8.0",
    "vitest": "^3.1.0"
  },
  "dependencies": {
    "dompurify": "^3.3.3",
    "lottie-web": "^5.13.0",
    "marked": "^17.0.5"
  }
}
